package data_structures.stack;

public class PostfixEvaluation {

    // scan from left to right and
    // whenever you see operand(binary) pop off 2 elements and do operation and put it in the data_structures.stack
    // when finished scanning the input in the end there is one element on top of data_structures.stack i.e Result
}
